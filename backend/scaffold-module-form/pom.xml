<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>scaffold</artifactId>
        <groupId>com.scaffold</groupId>
        <version>3.9.2</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>scaffold-module-form</artifactId>
    <description>
        通用表单引擎模块（M-10）：表单模板库 + 渲染器 + 提交记录混合存储 + 6 个高阶 widget。
        与 Workflow 模块的 wf_form_schema 互不越界 —— 后者是 流程节点表单，本模块是 业务通用表单。
        删除整个目录与 admin 中的依赖即下线，对其它模块零影响。
    </description>

    <dependencies>
        <!-- 复用框架核心：BaseController / @AuditLog / SecurityUtils / ScaffoldModule / UploadStorageService -->
        <dependency>
            <groupId>com.scaffold</groupId>
            <artifactId>scaffold-framework</artifactId>
        </dependency>

        <!-- Swagger / OpenAPI 注解；版本由父 pom dependencyManagement 管理 -->
        <dependency>
            <groupId>io.swagger.core.v3</groupId>
            <artifactId>swagger-annotations-jakarta</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.junit.vintage</groupId>
                    <artifactId>junit-vintage-engine</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
    </dependencies>

</project>
