<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scaffold.module.form.mapper.FormTemplateMapper">

    <resultMap id="templateMap" type="com.scaffold.module.form.domain.FormTemplate">
        <id     column="id"            property="id"/>
        <result column="form_key"      property="formKey"/>
        <result column="name"          property="name"/>
        <result column="category"      property="category"/>
        <result column="schema_json"   property="schemaJson"/>
        <result column="version"       property="version"/>
        <result column="status"        property="status"/>
        <result column="description"   property="description"/>
        <result column="published_at"  property="publishedAt"/>
        <result column="del_flag"      property="delFlag"/>
        <result column="create_by"     property="createBy"/>
        <result column="create_time"   property="createTime"/>
        <result column="update_by"     property="updateBy"/>
        <result column="update_time"   property="updateTime"/>
        <result column="remark"        property="remark"/>
    </resultMap>

    <sql id="cols">
        id, form_key, name, category, schema_json, version, status, description,
        published_at, del_flag, create_by, create_time, update_by, update_time, remark
    </sql>

    <sql id="commonWhere">
        <where>
            del_flag = '0'
            <if test="q.keyword != null and q.keyword != ''">
                AND name LIKE CONCAT('%', #{q.keyword}, '%')
            </if>
            <if test="q.category != null and q.category != ''">
                AND category = #{q.category}
            </if>
            <if test="q.status != null and q.status != ''">
                AND status = #{q.status}
            </if>
        </where>
    </sql>

    <select id="selectPage" resultMap="templateMap">
        SELECT <include refid="cols"/>
        FROM form_template
        <include refid="commonWhere"/>
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(1) FROM form_template
        <include refid="commonWhere"/>
    </select>

    <select id="selectById" resultMap="templateMap">
        SELECT <include refid="cols"/>
        FROM form_template
        WHERE id = #{id} AND del_flag = '0'
    </select>

    <select id="selectLatestByFormKey" resultMap="templateMap">
        SELECT <include refid="cols"/>
        FROM form_template
        WHERE form_key = #{formKey} AND del_flag = '0'
        ORDER BY version DESC
        LIMIT 1
    </select>

    <select id="selectAllByFormKey" resultMap="templateMap">
        SELECT <include refid="cols"/>
        FROM form_template
        WHERE form_key = #{formKey} AND del_flag = '0'
        ORDER BY version DESC
    </select>

    <insert id="insert" parameterType="com.scaffold.module.form.domain.FormTemplate"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO form_template
            (form_key, name, category, schema_json, version, status, description,
             published_at, del_flag, create_by, create_time, update_by, update_time, remark)
        VALUES
            (#{formKey}, #{name}, #{category}, #{schemaJson}, #{version}, #{status}, #{description},
             #{publishedAt}, '0', #{createBy}, NOW(), #{updateBy}, NOW(), #{remark})
    </insert>

    <update id="updateById" parameterType="com.scaffold.module.form.domain.FormTemplate">
        UPDATE form_template
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="category != null">category = #{category},</if>
            <if test="schemaJson != null">schema_json = #{schemaJson},</if>
            <if test="version != null">version = #{version},</if>
            <if test="status != null">status = #{status},</if>
            <if test="description != null">description = #{description},</if>
            <if test="publishedAt != null">published_at = #{publishedAt},</if>
            update_by = #{updateBy},
            update_time = NOW()
        </set>
        WHERE id = #{id} AND del_flag = '0'
    </update>

    <update id="softDeleteById">
        UPDATE form_template
        SET del_flag = '2', update_by = #{operator}, update_time = NOW()
        WHERE id = #{id} AND del_flag = '0'
    </update>

</mapper>
