<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scaffold.system.mapper.SysUserExternalIdentityMapper">

    <resultMap id="ExternalIdentityResult" type="com.scaffold.system.domain.SysUserExternalIdentity">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="provider" column="provider"/>
        <result property="subject" column="subject"/>
        <result property="email" column="email"/>
        <result property="rawProfile" column="raw_profile"/>
        <result property="boundAt" column="bound_at"/>
        <result property="lastLoginAt" column="last_login_at"/>
    </resultMap>

    <select id="selectByProviderAndSubject" resultMap="ExternalIdentityResult">
        SELECT id, user_id, provider, subject, email, raw_profile, bound_at, last_login_at
        FROM sys_user_external_identity
        WHERE provider = #{provider}
          AND subject  = #{subject}
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id"
            parameterType="com.scaffold.system.domain.SysUserExternalIdentity">
        INSERT INTO sys_user_external_identity
            (user_id, provider, subject, email, raw_profile, bound_at, last_login_at)
        VALUES
            (#{userId}, #{provider}, #{subject}, #{email}, #{rawProfile},
             COALESCE(#{boundAt}, NOW()), #{lastLoginAt})
    </insert>

    <update id="updateLastLoginAt">
        UPDATE sys_user_external_identity
        SET last_login_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
