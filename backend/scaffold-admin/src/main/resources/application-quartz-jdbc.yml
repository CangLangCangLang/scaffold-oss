spring:
  quartz:
    job-store-type: jdbc
    jdbc:
      initialize-schema: never
    properties:
      org.quartz.scheduler.instanceName: ScaffoldScheduler
      org.quartz.scheduler.instanceId: AUTO
      org.quartz.threadPool.class: org.quartz.simpl.SimpleThreadPool
      org.quartz.threadPool.threadCount: ${QUARTZ_THREAD_COUNT:20}
      org.quartz.threadPool.threadPriority: 5
      org.quartz.jobStore.class: org.springframework.scheduling.quartz.LocalDataSourceJobStore
      org.quartz.jobStore.isClustered: ${QUARTZ_CLUSTERED:true}
      org.quartz.jobStore.clusterCheckinInterval: 15000
      org.quartz.jobStore.maxMisfiresToHandleAtATime: 10
      org.quartz.jobStore.misfireThreshold: 12000
      org.quartz.jobStore.tablePrefix: QRTZ_
